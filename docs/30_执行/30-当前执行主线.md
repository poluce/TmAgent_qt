# 当前执行主线（唯一状态真源）

> 状态：active（执行中）
> 更新时间：2026-03-20
> 说明：本页只回答“当前在做什么 / 卡在哪 / 下一步是什么”。历史长记录已迁入 `docs/30_执行/36-执行日志归档.md`。

---

## 0、使用规则（必读）

1. 本文档是唯一“当前在做什么”的状态真源。
2. `docs/30_执行/35-轨道注册表.md` 只登记长期轨道，不替代本文档。
3. `docs/30_执行/31-候选变更池.md` 只处理新事项准入，不替代本文档。
4. `docs/30_执行/32-阶段2验收记录.md`、`docs/30_执行/33-MEM-M2验收记录.md`、`docs/30_执行/34-MEM-M3验收记录.md` 只记录验收结论，不替代本文档。
5. 历史长流水不再追加到本文档，统一迁入 `docs/30_执行/36-执行日志归档.md`。

---

## 一、当前产品阶段

1. 已完成并发布通过：阶段 2（多视角）
2. 下一产品阶段：阶段 3（群聊与 Agent 间通信）
3. 当前判定：阶段 3 尚未正式启动，只允许推进门禁和前置准备，不得把横向专项里程碑误写成产品阶段。

---

## 二、当前主线轨道

1. 轨道编号：`MEM-M4`
2. 轨道类型：`feature`
3. 当前状态：`in_progress`
4. 轨道目标：
   - 收口记忆系统 M4 的反思任务和质量评分
   - 完成混合检索策略调优与验收补强
   - 保持 Markdown 真源和派生索引边界稳定
   - 为后续插件消费统一记忆接口打基础
5. 完成定义：
   - `MEM-M4` 实现验收通过
   - `MEM-M4` 发布验收通过
   - 检索、反思、质量评分和回写链路形成稳定闭环
6. 当前依赖：
   - `PROC-DOC-01` 已关闭，文档执行机制已完成收口

---

## 三、当前迭代

1. 迭代编号：`R-2026-03-20-MEM-M4-RESTORE`
2. 输入来源：文档治理收口已完成，恢复记忆主线继续推进 M4。
3. 当前范围：
   - 记忆反思策略与质量评分调优
   - 检索排序与召回效果补强
   - `MEM-M4` 验收出口准备
   - `HB-H2` / `SCH-S1` 与记忆主线的边界保持一致
4. 非范围：
   - 再次调整文档治理框架
   - 启动阶段 3 正式群聊开发
   - 新增“架构优化专用轨道”
5. 当前主要风险：
   - `MEM-M4` 验收出口虽已建立，但尚未完成统一核验和正式结论回填
   - 心跳与调度专题已有代码和测试基础，但仍需避免在未统一核验前误判为“已验收通过”

---

## 四、挂起与下一主线

1. `COL-GATE`：群聊启动门禁推进
   - 当前状态：`blocked`
   - 阻塞原因：阶段 3 功能/数据/Chaos 门禁未全部通过
2. 下一主线候选：`HB-H2`
   - 切换条件：`MEM-M4` 至少完成实现验收，且不影响当前主链稳定性

---

## 五、并行专题

1. `HB-H2`：心跳策略收口
   - 当前状态：`in_progress`
   - 是否阻塞主线：否
   - 重点：重复回复抑制、watch signal 投递、端到端策略调优与验收
2. `SCH-S1`：定时任务基础收口
   - 当前状态：`in_progress`
   - 是否阻塞主线：否
   - 重点：CRUD、持久化恢复、到点触发与后续专题验收出口

---

## 六、门禁与阻塞

### 6.1 阶段 3（群聊）启动门禁

1. `GATE-01` 阶段口径统一：`通过`
2. `GATE-02` 功能/数据/Chaos 门禁看板：`部分通过`
3. `GATE-03` 子代理委派链路收口：`通过`
4. `GATE-04` 可观测性补强：`通过`

### 6.2 当前仍未通过的关键项

1. 群聊多 Agent 并发执行与顺序渲染未验收通过
2. 群聊定向打断未验收通过
3. Chaos 场景（并发写冲突 / 中途崩溃恢复 / 失败风暴 / 网络抖动）未完成连续验证

### 6.3 代码依据（本轮核对）

1. `MEM-M4`
   - `src/core/service/MemoryMaintenanceService.cpp` 已发出 `memory.reflected` / `memory.quality` 事件。
   - `src/core/tools/MemoryTool.cpp` 已落地 `memory_vector_index.json`。
   - `src/core/tools/BuiltinTools.h` 已注册 `memory_search` / `memory_reindex` / `session_search`。
   - `tests/tools/MemoryToolTest.cpp` 已覆盖 BM25、向量回退、长期记忆优先和近日报志优先。
2. `HB-H2`
   - `src/core/service/include/HeartbeatService.h`、`src/core/service/include/AgentPulse.h` 已存在。
   - `src/core/service/ChatService.cpp` 已发出 `heartbeat.completed` / `heartbeat.skipped`。
   - `tests/service/HeartbeatServiceTest.cpp` 与 `tests/service/HeartbeatEndToEndTest.cpp` 已存在。
3. `SCH-S1`
   - `src/core/service/include/SchedulerService.h` 已存在。
   - `src/core/service/background/BackgroundTaskCoordinator.cpp` 已发出 `scheduler.completed` / `scheduler.failed`。
   - `tests/service/SchedulerServiceTest.cpp` 已存在。
4. `COL-GATE`
   - `src/core/service/include/MessageRouter.h` 与 `src/core/service/conversation/MessageRouter.cpp` 已存在。
   - `src/core/service/conversation/ConversationEnqueueCoordinator.cpp` 已写入 `mentions` 并发出 `message_routed`。
   - `tests/service/MessageRoutingIntegrationTest.cpp` 已覆盖 `@Agent` / `@all` 和 `child_*` 追踪字段。
   - 当前仍未发现 `GroupTurnScheduler` / `ConcurrencyController` 代码实体，因此阶段 3 仍保持门禁前状态。

---

## 七、下一步执行顺序

1. 继续推进 `MEM-M4`，完成策略调优和验收出口回填。
2. 持续推进 `HB-H2` 与 `SCH-S1`，但不抢占主线判定。
3. `COL-GATE` 仅在所有门禁通过后才允许进入阶段 3 正式实施。
4. 待 `MEM-M4` 收口后，再评估是否启动“架构优化任务专用文档流”。

---

## 八、最近更新（仅保留近期）

1. 2026-03-20：完成 `PROC-DOC-01` 收口，文档执行机制已切换到“唯一状态真源 + 双结论验收 + 轨道注册表”模式，并恢复 `MEM-M4` 为当前功能主线。
2. 2026-03-16：补齐 `SchedulerServiceTest` 自动化回归，完善调度基础验证。
3. 2026-03-16：补齐群聊路由集成证据，验证 `message_routed` 与用户消息 `mentions` 写入。
4. 2026-03-16：补齐委派链路可追踪性验证，打通父回合/子代理关联字段反查。
5. 2026-03-16：补齐消息主链并发写入验证，确认 SQLite-first 主链在同一 session 下完整可回读。
6. 2026-03-15：运行时状态真源回写完成，`task_state:*` 与 `heartbeat_state:*` 统一写入 SQLite `app_state`。
7. 2026-03-14：SQLite 主链收尾完成，会话、身份、消息、事件与 UI 恢复状态切到 SQLite-first。
8. 2026-03-13：执行记录面板三阶段改造完成收口，完成事实分层与原文四层展示。
9. 2026-03-12：新增 Agent 助手与 Codex 职责边界方案，并落地 `TaskStateService` 最小闭环。
10. 2026-03-11：补齐心跳自动化回归与端到端验收，完成无变化静默、24h 重复抑制和记忆反思联动。

---

*文档版本：v4.0*
*创建日期：2026-02-11*
*更新日期：2026-03-20*
*关联文档：docs/00_规范/02-愿景落地路线图.md、docs/30_执行/31-候选变更池.md、docs/30_执行/35-轨道注册表.md*
