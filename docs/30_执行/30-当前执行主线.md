# 当前执行主线（唯一）

> 状态：active（执行中）
> 目的：提供唯一“正在做什么”的总面板，避免在多个方案文档之间来回切换。

---

## 0、口径对齐状态（必读）

1. 状态：`synced`（2026-03-09）
2. 判定：当前主线文档已完成第三轮口径收口，并补齐 2026-02-28 ~ 2026-03-09 期间已合入 `main` 的插单回写；后续继续通过“插单后强制回写三处”机制保持同步。
3. 本轮已对齐：
   - 默认提示词规则已强化（规划问句优先方案、后台任务跟进约束）。
   - `docs/10_方案/14-心跳与定时任务方案.md` 已完成边界重构（Heartbeat 与 Scheduler 职责拆分、里程碑改为 H/S 双序列）。
   - 阶段看板与下一步执行顺序已明确“记忆主线优先，心跳/调度并行专题收口”。
   - 2/28~3/2 已合并插单已回写：DeepSeek 专属 Provider / ThinkingIndicatorWidget / 交互式 CLI / SQLite 迁移过渡态（JSONL + SQLite 双写）/ 日志 SQLite 检索与 Windows 控制台输出修复。
4. 当前仍待落地（非文档偏差）：
   - 心跳/定时任务已完成首版代码与 UI 接线（`HealthMonitor/HeartbeatService/AgentPulse/SchedulerService` + `ChatService` + 设置页入口），当前待端到端验收与策略调优收口。
5. 执行约束（立即生效）：
   - 任一功能插单完成后，必须同步回写本文件“三处”：`当前阶段定位`、`当前迭代`、`最近更新记录`。
   - 任一主线切换前，先做一次“文档口径校准”，再进入下一步开发。
   - 若文档与代码口径冲突，以“代码可运行事实 + 验收记录”作为临时准绳，并在当日回写修正。

---

## 一、主线锚点

1. 主执行方案：`docs/10_方案/10-架构升级设计方案.md`
2. 阶段门禁：`docs/00_规范/02-愿景落地路线图.md`
3. 闭环规范：`docs/00_规范/03-文档执行闭环规范.md`

> 说明：本文件不替代方案文档，只负责“当前执行状态”和“下一步”。

---

## 二、当前阶段定位

1. 当前所在阶段：阶段 6 已收口，阶段 7 进行中（M4 首项）
2. 阶段 1 收口结果：
   - ChatService 持久化与回放职责下沉到 `ChatStateRepository` + `ChatPersistenceService`
   - Turn 管理已统一由 `TurnManager` 承担（队列/活动回合/深度查询）
   - 保存/加载链路完成编排层与存储层分离，行为保持不变
3. 阶段 2 收口结果：
   - 多视角切换、权限隔离、流式治理完成并通过验收
   - 验收记录见：`docs/30_执行/32-阶段2验收记录.md`
4. 当前目标：在不破坏主链路的前提下启动记忆 M4（质量评分与反思任务优先，向量检索后置）
5. 阶段说明：2026-02-28 ~ 2026-03-09 合入的 Provider / UI / CLI / 持久化 / 日志插单均视为“主链路增强”，不改变当前阶段判定。

---

## 三、当前迭代（唯一进行中）

1. 当前迭代编号：`R-2026-02-14-MEM1`
2. 上一迭代：`R-2026-02-13-A`（已中止，群聊主线后置）
3. `R-2026-02-11-A` 完成判定：
   - `src/core/service/ChatService.cpp` 继续拆分（已完成：状态持久化/加载职责下沉）
   - 关键链路回归通过（已完成：改动文件语法编译检查通过）
   - 文档回写完成（已完成：本文件回写）
4. `R-2026-02-11-B` 完成判定：
   - 阶段 2 功能正确性收口完成
   - 验收文档归档完成（`docs/30_执行/32-阶段2验收记录.md`）
5. `R-2026-02-13-A` 中止说明：
   - 原目标：阶段 3 启动（群聊/路由/调度骨架）
   - 中止原因：执行优先级调整为“先记忆系统，后群组协作”
6. `R-2026-02-14-MEM1` 目标：记忆 M1 落地（初始化文档、Recall 注入、Retain daily、原子写与错误事件）
7. `R-2026-02-14-MEM1` 当前进度：已完成代码接线与本地编译验证（MemoryManager/MemoryDocument + ChatService 回合前后挂接，`release/TmAgent.exe` 已成功链接）。
8. `R-2026-02-14-MEM1` 本轮补齐：Identity 持久化 ID 恢复（重启不换 Agent 目录）与“删除 Agent 同步删除记忆目录”已落地。
9. `R-2026-02-14-MEM1` 本轮补齐：新增“记忆”配置页（记忆管家助手/模型选择、用户画像字段编辑与回写 `user.md`、策略回写 `memory_policy.json`）。
10. `R-2026-02-14-MEM1` 本轮补齐：上下文预算治理落地（历史自动压缩、工具结果截断、回合 `context.compacted` 事件）。
11. `R-2026-02-14-MEM1` 本轮补齐：子代理治理落地（`delegate_task` 超时/长度限制、递归深度约束、子代理模型工厂与工具调度注入）。
12. `R-2026-02-14-MEM1` 本轮补齐：Agent 独立工作空间落地（每个 Agent 拥有 `identities/agents/<agent_id>/workspace`，工具调用自动注入并默认在各自工作空间执行）。
13. `R-2026-02-14-MEM1` 本轮补齐：M2 最小版落地（规则提炼写入 `memory.md`、指纹去重、识别“记住/remember this”手动记忆指令）。
14. `R-2026-02-14-MEM1` 本轮补齐：新增 `memory.compacted` 事件，用于标记长期记忆沉淀行为与数量。
15. `R-2026-02-14-MEM1` 本轮补齐：聊天气泡右键新增“记住这条”入口，直连后端手动记忆写入（不通过对话文本绕路）。
16. `R-2026-02-14-MEM1` 本轮补齐：`execute_command` 策略调整为“黑名单优先，白名单可选”（新增 `enforce_safe_prefixes` 开关，默认关闭），降低白名单维护成本与误拦截概率。
17. `R-2026-02-14-MEM1` 本轮补齐：工具循环总时长熔断优化（默认预算提升到 180000ms；当回合存在有效推进时不因总时长立即硬熔断），修复“长命令成功后仍被中断”问题。
18. `R-2026-02-14-MEM1` 本轮补齐：命令权限设置页新增“工具循环预算”可视化配置（轮次/重复/无进展/失败/总时长），统一落盘到 `tool_loop_policy.json`。
19. `R-2026-02-14-MEM1` 本轮补齐：`memory.updated/memory.compacted/memory.error` 已同步镜像到“请求/响应历史”右侧面板；`memory.error` 在聊天区也可见。
20. `R-2026-02-14-MEM1` 本轮补齐：手动“记住这条”在无活跃回合时补齐 `source_trace_id/source_turn_id` 与合成回合标识，保证 trace 反查链路完整。
21. `R-2026-02-14-MEM1` 本轮补齐：M2 提炼策略参数化（`auto_extract_enabled`、`min_user_chars_for_extract`、`max_long_memory_candidates_per_turn`）已打通到“信息设置”页与 `memory_policy.json`。
22. `R-2026-02-14-MEM1` 本轮补齐：阶段 5 验收记录归档（`docs/30_执行/33-阶段5验收记录.md`），主线切换至阶段 6（M3 启动）。
23. `R-2026-02-14-MEM1` 本轮补齐：阶段 6 启动首项 `memory_search` 工具（Markdown 检索可用版）落地，并自动注入 `_agent_id` 用于默认检索当前助手记忆。
24. `R-2026-02-14-MEM1` 本轮补齐：阶段 6 第二项落地：新增 `memory_reindex` 工具与 SQLite FTS 派生索引重建链路；`memory_search` 改为“索引优先、Markdown 回退”。
25. `R-2026-02-14-MEM1` 本轮补齐：记忆索引生命周期接线完成：`retainTurn/rememberManual` 成功后自动重建索引，并上报 `memory.index.updated` / `memory.index.error` 事件。
26. `R-2026-02-14-MEM1` 本轮补齐：信息设置页新增“一键重建记忆索引”按钮，支持用户侧手动重建并查看成功/失败统计。
27. `R-2026-02-14-MEM1` 本轮补齐：索引损坏自愈流程落地：当 SQLite 检索/重建命中“数据库损坏”错误时，自动删库并二次重建；`memory_search` 会自动修复后重试一次，降低“索引损坏即不可用”概率。
28. `R-2026-02-14-MEM1` 本轮补齐：阶段 6 验收记录归档（`docs/30_执行/34-阶段6验收记录.md`），M3 进入已验收状态。
29. `R-2026-02-14-MEM1` 本轮补齐：阶段 7 首项落地（M4 启动）：新增“按回合间隔触发”的记忆反思任务与质量评分，产出 `memory.reflected` / `memory.quality` 事件并在必要时刷新索引。
30. `R-2026-02-14-MEM1` 本轮补齐：新增 `session_search` 工具（会话历史检索），用于找回未沉淀到记忆文档的历史事实；默认提示词增加“上下文缺失时先 `memory_search`，再 `session_search`”约束。
31. `R-2026-02-14-MEM1` 本轮补齐：信息设置页补齐 M4 反思策略配置入口，支持直接管理 `reflect_enabled` / `reflect_every_n_turns` / `reflect_max_candidates_per_run` / `reflect_scan_daily_files`，让已落地的反思与质量评分链路可通过 UI 调优。
32. `R-2026-02-14-MEM1` 本轮补齐：子代理委派执行链路做架构收口：新增 `DelegateTaskScheduler` 统一承接超时/停滞/护栏与快照观测，`AgentTool` 改为薄适配层，`LLMAgent` 移除 `delegate_task` 专用补参逻辑，避免多处策略漂移。
33. `R-2026-02-14-MEM1` 本轮补齐：默认提示词新增“意图优先级”约束（用户问“怎么做/方案/步骤”时先给方案，未确认不执行），并限制后台任务查询仅在用户明确要求时触发。
34. `R-2026-02-14-MEM1` 本轮补齐：心跳与定时任务方案文档完成职责拆分与口径重构（Heartbeat 粗粒度维护、Scheduler 精确调度，里程碑改为 H0-H2 + S1-S2）。
35. `R-2026-02-14-MEM1` 本轮补齐：心跳/调度首版代码骨架落地：新增 `HealthMonitor`（Provider 可达性探测）、`AgentPulse`（主代理状态心跳）、`HeartbeatService`（周期唤醒 + 合并触发）与 `SchedulerService`（Cron 任务触发）并接入 `ChatService` 事件流。
36. `R-2026-02-14-MEM1` 本轮补齐：新建/加载 Agent 自动初始化 `heartbeat_config.json` 与 `HEARTBEAT.md` 模板，支持无 UI 条件下直接文件配置验证。
37. `R-2026-02-14-MEM1` 当前缺口：心跳/调度待端到端验收与策略调优（H1/S1 已完成 UI 接线；H2/S2 进入“已启动，待收口”）。
38. `R-2026-02-14-MEM1` 本轮补齐：信息设置页新增心跳配置与定时任务配置入口（默认开启），支持“启用、间隔、活跃时段、时区、HEARTBEAT.md、立即触发、任务 CRUD”。
39. `R-2026-02-14-MEM1` 本轮补齐：心跳默认策略改为“无变化静默 + 仅变化通知 + 最小通知间隔”，并新增 `heartbeat_state.json` 快照摘要用于变化判定与限频。
40. `R-2026-02-14-MEM1` 本轮补齐：心跳 Watchpoint 信号改为 UI 可配置（Provider/子代理任务/Pulse/定时任务/记忆进度 + 扩展信号），状态面板同步显示当前快照信号集合。
41. `R-2026-02-14-MEM1` 本轮补齐：Provider / 思考态链路增强：新增 `DeepSeekProvider`，`ChatService -> MainWindow -> IdentityView` 打通 `reasoningStarted/reasoningStopped` 信号，`ThinkingIndicatorWidget` 用于展示深度思考与工具调用状态。
42. `R-2026-02-14-MEM1` 本轮补齐：新增 `TmAgentCli --interactive` 交互式 CLI（REPL）模式；CLI 回合结束强制落盘，会话侧自动注入活跃子代理任务列表，并在子任务完成时写入系统级通知。
43. `R-2026-02-14-MEM1` 本轮补齐：会话/事件持久化进入 SQLite 迁移过渡态：新增 `DatabaseManager`（WAL、建表/迁移、线程安全连接），`ChatPersistenceService` 支持 `JSONL + SQLite` 双写与数据库读写接口。
44. `R-2026-02-14-MEM1` 本轮补齐：多进程消息同步落地：`ChatService` 新增外部变更轮询，`Session::findMessageById` 用于去重，GUI 侧通过 `sync_messages_injected` 事件自动刷新跨进程新增消息；该能力视为迁移期增强，而非全量切库完成。
45. `R-2026-02-14-MEM1` 本轮补齐：日志链路升级：`tmagent-log` / CLI 日志查询优先走 SQLite 扫描器，Windows 控制台强制 UTF-8 并通过全局互斥锁缓解多线程输出中文乱码与交错。

---

## 四、阶段进度看板

1. 阶段 0：已完成
2. 阶段 1：已完成
3. 阶段 2：已完成
4. 阶段 3：已完成（记忆 M1 基础闭环）
5. 阶段 4：已完成（记忆事件观测闭环）
6. 阶段 5：已完成（M2 验收通过）
7. 阶段 6：已完成（M3 验收通过）
8. 阶段 7：进行中（M4 首项：反思任务 + 质量评分）
9. 并行专题 A（心跳/调度）：方案口径已收口，H0/H1/S1 已完成代码+UI 接线，当前进入 H2/S2 验收收口

> 口径说明：以上“阶段 3-7”是当前记忆主线的执行编号；不等同于《愿景落地路线图》中的“阶段 3=群聊协作”。

---

## 五、阶段与记忆里程碑映射（唯一口径）

1. 阶段 1-2：不落记忆功能，仅保持记忆所需基础边界（身份/会话分离、Service 组装边界、可追踪 ID）。
2. 阶段 3：落地 M1（基础闭环）：
   - 初始化记忆文档（`soul.md`/`identity.md`/`memory.md`/`memory/YYYY-MM-DD.md`）。
   - 回合前 Recall 注入。
   - 回合后 daily 追加与原子写。
3. 阶段 4：补齐 M1 观测闭环：
   - `memory.updated` / `memory.compacted` / `memory.error` 进入 trace/event 链路。
   - 可通过 `trace_id` 反查记忆写入来源。
4. 阶段 5：完成 M2（提炼、去重、长期沉淀、“记住这条”入口）。
5. 阶段 6：完成 M3（FTS5 派生索引、`memory_search`、索引重建）。
6. 阶段 7：完成 M4（向量检索、混合排序、质量评分与反思任务），并通过统一接口供插件消费。
7. 约束：不得因提前引入 M3/M4 破坏阶段 3 主线交付。

---

## 六、阶段口径对齐（新增）

1. 路线图口径（规范）：`docs/00_规范/02-愿景落地路线图.md`
   - 阶段 3 = 群聊与 Agent 间通信（私聊/群聊/@/路由/调度）。
2. 当前执行口径（主线）：记忆 M1-M4 的收口节奏（本文件“阶段 3-7”）。
3. 现状判定（唯一）：
   - 记忆主线：阶段 7 进行中（M4 首项已落地）。
   - 群聊主线：未启动（后置，尚未进入正式实施）。
4. 进入群聊开发前，必须先通过“群聊前阻塞清单”。

---

## 七、群聊前阻塞清单（可打勾）

1. `GATE-01` 阶段口径统一（防止“阶段 3 已完成”语义冲突）
   - 状态：`[x]` 已定义并生效（本节 + 第四节口径说明）。
2. `GATE-02` 群聊前门禁看板化（功能/数据/Chaos 不靠口头判断）
   - 状态：`[x]` 已完成（已落地勾选看板，持续回写）
   - 要求：将 `docs/10_方案/10-架构升级设计方案.md` 的 6.2/6.3/6.6 与 7.4 门禁转为勾选清单并持续回写。
3. `GATE-03` 子代理委派链路收口（群聊前阻塞）
   - 状态：`[x]` 已完成
   - 要求：
     - `delegate_task` 对新建与历史助手均稳定可见（工具白名单不丢）。
     - 子代理执行链路可追踪（父回合/子回合关联字段可反查）。
     - 委派失败不影响主回合稳定推进（超时/取消/熔断路径一致）。
4. `GATE-04` 可观测性补强（面向协作排障）
   - 状态：`[x]` 已完成
   - 要求：
     - 右侧历史面板能快速定位“谁委派了谁、为何失败、耗时多少”。
     - 增加委派成功率、失败原因分布、平均耗时三项基础指标。

### 7.1 GATE-02 门禁看板（来自架构升级方案 6.2 / 6.3 / 6.6 / 7.4）

1. 功能正确性（6.2）
   - `[ ]` 群聊路由：`@Agent` / `@all` 路由结果正确（含自动化验证；`MessageRouter` 单测已落地，`message_routed` 事件与 `mentions` 已接线，待多 Agent 调度接入）
   - `[ ]` 并发输出：群聊多 Agent 可并发执行并顺序渲染
   - `[ ]` 定向打断：仅中断目标 Agent，不误伤其他 Agent
   - `[x]` Private 回归：私聊链路不受群聊演进影响（现有链路稳定）
2. 数据正确性（6.3）
   - `[ ]` `messages.jsonl` 在多 Agent 并发写场景下无乱序/半行
   - `[ ]` `turn_id/run_id/request_trace_id` 跨父子代理、跨工具链路可反查
   - `[x]` 委派链路字段可观测（`child_request_id/child_trace_id/child_agent_id`）
3. Chaos 验收（6.6）
   - `[ ]` 并发写冲突压测通过
   - `[ ]` 中途崩溃恢复通过（含 pending turn 恢复一致性）
   - `[ ]` 工具超时/失败风暴下系统可自稳
   - `[ ]` 网络抖动/5xx 重试策略达标
4. 进入阶段三门槛（7.4）
   - `[ ]` 6.2 全部通过
   - `[ ]` 6.3 全部通过
   - `[ ]` 6.6 四类 Chaos 连续 3 轮稳定通过

---

## 八、执行规则（防止 1/2 来回）

1. 任一时刻只允许一个“当前迭代编号”。
2. 新想法不直接改主线，先进入 `docs/30_执行/31-候选变更池.md`。
3. 只有当“当前迭代”完成或被明确中止，才允许切换下一主线。
4. 每次切换主线必须记录：切换原因、放弃项、继承项。

---

## 九、主线 + 插单机制（测试期生效）

1. 主线不变：任何时刻只维护一个“当前迭代编号”，持续推进直到完成或中止。
2. 插单入口：测试中发现的问题和临时想法，允许随时插入，不要求等当前迭代结束。
3. 插单分级：
   - `P0`（阻塞/崩溃/数据错乱/安全风险）：立即打断主线，先修复。
   - `P1`（功能异常但可继续）：插入当前迭代执行，修复后回到主线任务。
   - `P2`（体验优化/非阻塞想法）：进入候选池，排入下一迭代。
4. 插单最小信息要求（缺一不可）：
   - 现象描述
   - 复现步骤
   - 影响范围
   - 验收标准
5. 打断主线时必须回写三项：
   - 打断原因
   - 影响项（延后/取消/继承）
   - 恢复点（修复后从哪一条继续）
6. 所有插单（无论是否立即处理）都先登记 `docs/30_执行/31-候选变更池.md`，再执行。
7. 修复完成后必须同步更新本文件“最近更新记录”，确保当前状态可见。

---

## 十、下一步执行顺序（已重排）

1. 继续推进记忆主线阶段 7（M4 收口），保持单主线执行。
2. 群聊仍按 `GATE-01~04` 门禁推进：全部通过后再正式启动。
3. 心跳/调度维持“并行专题”节奏：不抢占记忆主线优先级，但持续推进 H2/S2 收口与验收。

---

## 十一、最近更新记录

1. 2026-02-11：建立执行层目录与唯一主线机制。
2. 2026-02-11：完成阶段 1 收口（ChatService 持久化/回放职责下沉到仓储层）。
3. 2026-02-12：阶段 2 稳定化进行中：流式 delta 仅路由活跃视图；turn 完成落盘改为单次触发，避免多视角重复保存。
4. 2026-02-13：阶段 2 验收通过，主线切换到阶段 3 准备。
5. 2026-02-14：新增“阶段 1-7 与记忆 M1-M4 映射”作为唯一执行口径。
6. 2026-02-14：执行优先级调整为“先记忆系统，群组协作后置”，当前迭代切换到 `R-2026-02-14-MEM1`。
7. 2026-02-14：M1 代码接线完成：新增 Markdown 记忆读写组件，并在回合前注入/回合后留存链路打通。
8. 2026-02-14：M1 本地构建验收通过：完整 clean + rebuild 成功，生成 `release/TmAgent.exe`。
9. 2026-02-14：补齐 M1 关键缺口：Agent ID 持久化恢复，删除 Agent 时同步删除其记忆目录（失败则中止删除）。
10. 2026-02-14：记忆模型升级为“公共+私有”双层：新增 `shared_work.md`、`user_view.md` 与 `memory_policy.json`（记忆管家写公共记忆）。
11. 2026-02-14：记忆配置页落地：可在 UI 中配置记忆管家与模型，并维护用户公共画像（回写 `user.md`）。
12. 2026-02-15：完成“长会话稳定性 + 委派稳定性”补强：历史预算压缩、请求消息裁剪、`delegate_task` 超时与输出治理、本地工具注册替换时内存泄漏修正。
13. 2026-02-15：完成 Agent 工作空间隔离：`LLMConfig.workspaceDir` 注入、Shell/File 工具默认绑定 Agent 工作空间并限制目录越界。
14. 2026-02-15：记忆 M2 最小版接线：回合后基于规则提炼长期记忆并写入 `memory.md`，通过 `[fp:hash]` 指纹去重，支持“记住/remember this”手动触发。
15. 2026-02-15：新增 `memory.compacted` 事件，便于在请求-响应历史中直接观察长期记忆沉淀结果。
16. 2026-02-15：补齐 M2 手动入口：聊天消息右键“记住这条”可直接写入 `memory.md`，并上报 `memory.updated/memory.compacted` 事件。
17. 2026-02-16：命令策略改为默认黑名单优先，新增 `enforce_safe_prefixes` 可选开关；命令权限配置页新增“命令准入”控制项。
18. 2026-02-16：工具循环时长熔断策略优化：默认预算提升到 3 分钟，且在“有成功推进”场景下不立即硬熔断，避免 clone/构建后被误中断。
19. 2026-02-16：命令权限设置页扩展为“双策略面板”（命令权限 + 工具循环预算），支持通过 UI 调整工具熔断阈值并即时生效于下一轮调用。
20. 2026-02-17：记忆事件已进入右侧“请求/响应历史”面板（含 `memory.error` 可视化），并补齐手动记忆无活跃回合时的 trace/turn 追踪字段。
21. 2026-02-17：记忆长期提炼规则改为可配置，并完成 UI 配置入口：可开关自动提炼、设置最小提炼长度和每回合长期记忆上限。
22. 2026-02-17：阶段 5 验收文档归档（`docs/30_执行/33-阶段5验收记录.md`），阶段看板更新为“阶段 5 已完成，阶段 6 进行中”。
23. 2026-02-17：阶段 6 首项落地：新增 `memory_search` 工具与 schema；LLMAgent 工具调用链自动注入 `_agent_id`，支持默认检索当前助手记忆。
24. 2026-02-17：阶段 6 第二项落地：新增 `memory_reindex` 与 SQLite FTS 派生索引重建流程；`memory_search` 支持索引自动重建与 Markdown 回退，检索稳定性提升。
25. 2026-02-17：阶段 6 第三项落地：记忆写入后自动触发索引重建，并将索引成功/失败事件纳入统一事件流与请求/响应历史面板。
26. 2026-02-17：阶段 6 第四项落地：信息设置页增加“重建记忆索引”入口，用户可直接触发全量重建并获得结果摘要。
27. 2026-02-18：阶段 6 补强：索引损坏自动自愈（删库重建 + 查询重试）接线完成，`memory_search`/`memory_reindex` 对损坏索引的恢复能力提升。
28. 2026-02-18：阶段 6 验收文档归档（`docs/30_执行/34-阶段6验收记录.md`），主线阶段定位切换为“阶段 6 已收口，阶段 7 待启动”。
29. 2026-02-18：阶段 7 启动首项：回合后按策略执行记忆反思与质量评分（默认每 8 个 retain 回合一次），新增 `memory.reflected` / `memory.quality` 事件并接入索引刷新。
30. 2026-02-18：阶段 7 补齐“历史找回”能力：新增 `session_search` 检索 `messages.jsonl`，并将“缺失先检索（memory_search -> session_search）”纳入默认系统提示词。
31. 2026-02-18：执行机制升级为“主线 + 插单”：按 `P0/P1/P2` 分级处理测试期问题，支持随时插单但保持单主线推进，并要求插单最小信息与回写闭环。
32. 2026-02-18：新增“阶段口径对齐 + 群聊前阻塞清单（GATE-01~04）+ 下一步执行顺序”，明确群聊后置但不丢门禁。
33. 2026-02-18：完成 `GATE-03/GATE-04` 收口：`delegate_task` 对新旧助手稳定可见，补齐父/子委派关联字段（`child_request_id/child_trace_id/child_agent_id`），并新增委派成功率/失败数/平均耗时事件指标。
34. 2026-02-21：完成 `GATE-02` 看板化：将 6.2/6.3/6.6/7.4 转为勾选清单并在主线文档持续回写；委派完成事件补齐 `child_timeline/child_tools/child_error` 可观测字段。
35. 2026-02-21：新增 `MessageRouter`（@提及解析 + 路由决策）与 `tests/service/MessageRouterTest`，先完成可独立验证，再接入群聊执行主链路。
36. 2026-02-21：`ChatService::enqueueUserMessageAs` 接入 `MessageRouter`：用户消息写入时同步记录 `mentions` 与 `message_routed` 事件（当前仅观测接线，不改变单 Agent 执行路径）。
37. 2026-02-21：子代理治理从“工具内部临时事件循环”升级为“独立调度器”：新增 `src/core/agent/DelegateTaskScheduler.*`，并完成 `AgentTool` 薄封装化，委派护栏和快照观测统一到调度层。
38. 2026-02-25：新增“口径对齐状态（pending_sync）”与文档回写强约束，明确当前主线文档存在进度滞后风险并要求每轮插单后回写三处（阶段定位/当前迭代/最近更新记录）。
39. 2026-02-25：完成第一轮口径校准：主线文档同步“提示词强化”与“心跳/定时任务边界重构”，并明确心跳/调度能力尚处方案阶段，未进入源码实现。
40. 2026-02-25：完成第二轮口径校准：阶段看板新增“并行专题 A（心跳/调度）”状态，下一步执行顺序明确“记忆主线优先、心跳/调度先保持方案态”；候选池补录 `C-20260225-01/02` 并标记 accepted。
41. 2026-02-25：完成第三轮口径收口：`口径对齐状态` 从 `syncing` 切换为 `synced`，并将“心跳/调度未实现”归类为代码待落地项而非文档偏差。
42. 2026-02-25：并行专题 A 进入代码态：新增 `HealthMonitor/HeartbeatService/HeartbeatWake/AgentPulse/SchedulerService` 与 `ChatService` 接线，实现心跳触发、定时任务触发、Provider 健康探测与状态事件上报的首版闭环。
43. 2026-02-25：补齐心跳默认配置初始化：Agent 目录自动生成 `HEARTBEAT.md` 与 `heartbeat_config.json`，可直接通过文件启用心跳巡检。
44. 2026-02-26：补齐心跳/定时任务设置页接线：支持 UI 侧修改心跳间隔、活跃时段、时区、心跳内容，并支持定时任务 CRUD/立即执行。
45. 2026-02-26：完成心跳消息降噪策略：默认“无变化静默 + 仅变化通知 + 最小通知间隔”，并写入 `heartbeat_state.json` 快照用于变化判定与限频。
46. 2026-02-26：心跳链路升级为“高频采样 + 低频落盘”：支持秒级采样（最小 5 秒），并通过内存态缓存与 `statePersistIntervalMs` 限制无变化场景的磁盘写入频率。
47. 2026-02-26：信息设置页新增“心跳状态（只读）”小面板（上次快照/通知/变化/原因/摘要/活跃任务），支持手动刷新与 3 秒自动刷新，减少文件排障成本。
48. 2026-02-26：心跳设置页补齐“低频落盘”可视化控制（无变化仍落盘开关 + 状态落盘最小间隔秒级配置），完成分层策略全链路 UI 化。
49. 2026-02-26：心跳 Watchpoint 进一步可视化：设置页新增“快照信号/扩展信号”配置，`heartbeat_state.json` 与状态面板同步展示 `watch_signals`，支持变化驱动通知按需调优。
50. 2026-02-28：新增 `DeepSeekProvider` 与思考态事件透传，`ThinkingIndicatorWidget` 接入 `IdentityView/MainWindow`，深度思考与工具调用状态可在 UI 中独立展示。
51. 2026-03-02：新增 `TmAgentCli --interactive` 交互式 CLI 模式，补齐子代理任务反馈与活跃任务上下文注入，CLI / GUI 可共享主链路能力。
52. 2026-03-02：会话与事件持久化进入 SQLite 迁移阶段：当前采用 `JSONL + SQLite` 双写；新增 `DatabaseManager`、`pollExternalChanges` 与 `sync_messages_injected`，完成跨进程消息同步闭环，但尚未完成全量切换。
53. 2026-03-02：日志检索与会话列表加载切换为 SQLite 优先路径；CLI / `tmagent-log` 在 Windows 下强制 UTF-8 并通过互斥锁减少输出交错与中文乱码。
54. 2026-03-09：`cli_llm_log_test` 已经重新合入 `main`（PR #5），本文件同步回写主线阶段口径、当前迭代补齐项与最近更新记录，恢复“代码事实 = 文档口径”。
55. 2026-03-10：继续推进记忆主线 M4 收口：信息设置页补齐反思任务策略配置，可直接调整反思开关、触发间隔、候选上限与扫描天数，减少“后端已支持、UI 不可配”的口径落差。

---

*文档版本：v2.8*
*创建日期：2026-02-11*
*关联文档：docs/10_方案/10-架构升级设计方案.md、docs/00_规范/02-愿景落地路线图.md、docs/30_执行/31-候选变更池.md*
