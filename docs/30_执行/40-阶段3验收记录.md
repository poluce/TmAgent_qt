# 阶段3验收记录（群聊与 Agent 间通信）

> 状态：draft（门禁准备中）
> 日期：2026-03-20（按代码现状同步）
> 实现验收：未开始
> 发布验收：未开始
> 依据：`docs/30_执行/30-当前执行主线.md`、`docs/00_规范/02-愿景落地路线图.md`、`docs/10_方案/10-架构升级设计方案.md`

---

## 1. 验收范围

1. 群聊创建、成员管理和归档能力可用。
2. `@Agent` / `@all` 路由结果正确。
3. 多 Agent 并发执行与顺序渲染可用。
4. 定向打断、数据正确性和 Chaos 门禁全部通过。

## 2. 验收项结果

1. `MessageRouter` 已落地。
代码依据：`src/core/service/include/MessageRouter.h`、`src/core/service/conversation/MessageRouter.cpp`
2. 用户消息 `mentions` 写入与 `message_routed` 事件已落地。
代码依据：`src/core/service/conversation/ConversationEnqueueCoordinator.cpp`
3. 群聊路由与 `child_*` 追踪字段已有集成测试覆盖。
代码依据：`tests/service/MessageRoutingIntegrationTest.cpp`
4. 当前仍未发现 `GroupTurnScheduler` / `ConcurrencyController` 代码实体，因此阶段 3 仅处于门禁预备状态。

## 3. 结论说明

1. 本记录已作为阶段 3 的固定验收出口建立。
2. 当前代码只能证明阶段 3 的门禁预备能力已存在，不能证明阶段 3 已启动。
3. 在门禁、证据和平台回归完成前，不得将阶段 3 标记为实现验收通过或发布验收通过。

## 4. 风险与后续

1. 风险：阶段 3 仍受功能、数据和 Chaos 三类门禁约束。
2. 后续：待 `COL-GATE` 通过后补齐正式验收内容。

---

*文档版本：v1.0*
*创建日期：2026-03-20*
