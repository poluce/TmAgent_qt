# HB-H2 验收记录（心跳策略收口）

> 状态：draft（验收准备中）
> 日期：2026-03-20（按代码现状同步）
> 实现验收：进行中（代码已落地，待统一核验）
> 发布验收：未开始
> 依据：`docs/30_执行/30-当前执行主线.md`、`docs/10_方案/14-心跳与定时任务方案.md`

---

## 1. 验收范围

1. 24h 重复回复抑制有效。
2. 手动触发入口和配置热加载可用。
3. 心跳状态持久化与重启恢复有效。
4. 心跳与记忆反思联动成立，且不打断用户主动对话。

## 2. 验收项结果

1. `HeartbeatService` / `AgentPulse` 已落地。
代码依据：`src/core/service/include/HeartbeatService.h`、`src/core/service/include/AgentPulse.h`
2. `heartbeat.completed` / `heartbeat.skipped` 事件已落地。
代码依据：`src/core/service/ChatService.cpp`
3. 心跳端到端和服务级测试已存在。
代码依据：`tests/service/HeartbeatEndToEndTest.cpp`、`tests/service/HeartbeatServiceTest.cpp`

## 3. 结论说明

1. 本记录已作为 `HB-H2` 的固定验收出口建立。
2. 当前代码已能证明 `HB-H2` 核心能力和测试基础存在，但尚未完成统一核验，因此不能标记为实现验收通过。
3. 在正式核验和平台回归完成前，不得将 `HB-H2` 标记为发布验收通过。

## 4. 风险与后续

1. 风险：策略调优和跨场景稳定性仍需进一步验证。
2. 后续：待补齐正式证据、端到端执行结果和平台回归后更新本记录。

---

*文档版本：v1.0*
*创建日期：2026-03-20*
