# 候选变更池

> 状态：active（待评估）
> 目的：承接新想法，避免打断当前主线执行。

---

## 一、使用规则

1. 所有新想法先登记在这里。
2. 候选项状态仅允许：`new` / `accepted` / `rejected` / `deferred`。
3. 只有 `accepted` 项才能进入下一迭代主线。
4. 调研层文档不自动进入候选池，需先转换为可执行变更条目。
5. 测试中发现问题可作为“插单”登记，按优先级分为 `P0` / `P1` / `P2`。
6. `P0` 可直接打断主线执行；`P1` 可插入当前迭代；`P2` 默认下一迭代处理。
7. 插单执行前必须补齐“现象、复现步骤、影响范围、验收标准”四项。

---

## 二、候选项模板

1. 编号：`C-YYYYMMDD-XX`
2. 标题：
3. 来源：用户需求 / 调研 / 问题复盘
4. 类型：功能 / 缺陷 / 优化 / 架构
5. 优先级：`P0` / `P1` / `P2`
6. 是否插单：是 / 否
7. 是否打断主线：是 / 否（仅 `P0` 默认是）
8. 影响阶段：
9. 影响文档：
10. 影响代码模块：
11. 现象：
12. 复现步骤：
13. 影响范围：
14. 验收标准：
15. 风险：
16. 预估收益：
17. 状态：`new`
18. 结论：
19. 主线恢复点（如发生打断时必填）：

---

## 三、当前候选列表

1. `C-20260211-01`：完善阶段 1 收口的模块拆分追踪表
   - 来源：执行复盘
   - 类型：架构
   - 优先级：P2
   - 是否插单：否
   - 是否打断主线：否
   - 影响阶段：阶段 1
   - 状态：new
2. `C-20260211-02`：增加“阶段切换门禁检查清单”自动检查脚本
   - 来源：流程治理
   - 类型：优化
   - 优先级：P2
   - 是否插单：否
   - 是否打断主线：否
   - 影响阶段：阶段 1 -> 2
   - 状态：new
3. `C-20260218-01`：阶段口径对齐（路线图阶段 vs 记忆执行阶段）
   - 来源：问题复盘
   - 类型：架构
   - 优先级：P1
   - 是否插单：是
   - 是否打断主线：否
   - 影响阶段：全阶段（流程治理）
   - 现象：阶段编号在不同文档语义冲突，导致“是否进入群聊”判断不一致
   - 复现步骤：对比 `02-愿景落地路线图` 与 `30-当前执行主线` 的阶段 3 定义
   - 影响范围：执行决策、验收口径、团队沟通
   - 验收标准：主线文档明确双口径与唯一判定；后续评审不再出现阶段语义冲突
   - 状态：accepted
4. `C-20260218-02`：群聊前门禁清单可视化（功能/数据/Chaos）
   - 来源：流程治理
   - 类型：优化
   - 优先级：P1
   - 是否插单：是
   - 是否打断主线：否
   - 影响阶段：群聊启动前
   - 现象：群聊前提条件分散在方案文档，缺少统一勾选看板
   - 复现步骤：检查 `10-架构升级设计方案` 的门禁条目是否可在主线直接判定
   - 影响范围：群聊是否可启动、回归成本
   - 验收标准：主线文档提供可打勾门禁并持续回写状态
   - 状态：accepted
5. `C-20260218-03`：子代理委派链路收口（群聊前阻塞）
   - 来源：用户测试反馈
   - 类型：缺陷
   - 优先级：P1
   - 是否插单：是
   - 是否打断主线：否
   - 影响阶段：阶段 7 -> 群聊前
   - 现象：助手倾向自执行，不稳定委派子代理；委派链路存在可见性/一致性问题
   - 复现步骤：给助手复杂任务，观察工具调用与子代理执行路径
   - 影响范围：任务完成质量、后续群聊协作稳定性
   - 验收标准：`delegate_task` 稳定可见且可执行；父子回合可追踪；失败路径可恢复
   - 状态：accepted
6. `C-20260218-04`：委派可观测性补强（指标 + 快速定位）
   - 来源：问题复盘
   - 类型：优化
   - 优先级：P2
   - 是否插单：否
   - 是否打断主线：否
   - 影响阶段：群聊前
   - 现象：当前排障可见信息不足，难快速定位委派失败原因
   - 复现步骤：分析委派失败会话，检查面板是否能直接看出父子关系和失败分类
   - 影响范围：排障效率、回归验证
   - 验收标准：可看到委派成功率/失败原因/耗时；可用 trace 快速反查父子链路
   - 状态：new
7. `C-20260225-01`：心跳与定时任务边界重构（机制拆分）
   - 来源：用户需求澄清
   - 类型：架构
   - 优先级：P1
   - 是否插单：是
   - 是否打断主线：否
   - 影响阶段：全阶段（方案与执行口径）
   - 现象：心跳与定时任务语义混用，容易误解为“靠高频心跳实现精确定时”。
   - 复现步骤：查看 `14-心跳与定时任务方案` 中“心跳”和“定时任务”叙述是否可独立落地。
   - 影响范围：架构设计、实现优先级、性能预算
   - 验收标准：文档明确两套机制职责边界；里程碑拆分为心跳 H 系列与调度 S 系列；联动规则可执行
   - 状态：accepted
   - 结论：已落地到 `docs/10_方案/14-心跳与定时任务方案.md`，进入持续执行与评审阶段
8. `C-20260225-02`：执行主线文档口径校准机制
   - 来源：流程治理
   - 类型：优化
   - 优先级：P1
   - 是否插单：是
   - 是否打断主线：否
   - 影响阶段：全阶段（文档治理）
   - 现象：代码推进快于文档回写，主线状态与真实进度出现偏差。
   - 复现步骤：对比近期代码提交/实际改动与 `30-当前执行主线` 的阶段与迭代记录。
   - 影响范围：评审口径、阶段判断、下一步决策
   - 验收标准：主线文档新增“口径对齐状态”；插单后强制回写三处；更新记录可追溯
   - 状态：accepted
   - 结论：二轮校准与收口已落地（`pending_sync` -> `syncing` -> `synced`）；主线文档已建立常态化回写约束，后续按插单节奏持续维护
9. `C-20260225-03`：心跳/定时任务首版代码骨架落地
   - 来源：执行推进
   - 类型：架构
   - 优先级：P1
   - 是否插单：是
   - 是否打断主线：否
   - 影响阶段：并行专题 A（心跳/调度）
   - 现象：此前仅有方案文档，缺少可运行代码与事件接线，无法验证调度行为。
   - 复现步骤：查看 `src` 中不存在 `HealthMonitor/HeartbeatService/SchedulerService/AgentPulse`。
   - 影响范围：心跳能力、定时任务能力、事件可观测性
   - 验收标准：完成服务类实现并接入 `ChatService` 初始化与事件流；可通过配置文件触发心跳和定时任务
   - 状态：accepted
   - 结论：已落地首版骨架（含 `HeartbeatWake` 合并触发、Cron 调度基础、Provider 健康探测与 pulse 状态事件），后续进入 UI 配置与验收收口阶段
10. `C-20260312-01`：Agent 助手与 Codex 职责边界方案
   - 来源：产品定位澄清
   - 类型：架构
   - 优先级：P1
   - 是否插单：是
   - 是否打断主线：否
   - 影响阶段：全阶段（产品定位 / 能力建设优先级）
   - 现象：若不明确“人 / 助手 / Codex”边界，后续能力建设容易在“做桥梁层”与“做执行器替代品”之间摇摆。
   - 复现步骤：对比当前项目能力讨论与 `Codex` 更新方向，观察是否出现“是否要替代 Codex”的目标混乱。
   - 影响范围：产品路线、执行编排设计、权限边界、记忆承接方式
   - 验收标准：形成正式方案文档，明确三层职责边界、助手应向 `Codex` 学的能力、后续能力优先级，并挂接到主线执行文档
   - 状态：accepted
   - 结论：已落地到 `docs/10_方案/15-Agent助手与Codex职责边界方案.md`，后续 Agent 能力设计以“桥梁层优先”作为统一准绳
11. `C-20260312-02`：任务状态机与执行编排最小闭环
   - 来源：职责边界方案落地
   - 类型：架构
   - 优先级：P1
   - 是否插单：是
   - 是否打断主线：否
   - 影响阶段：阶段 7 / Agent 助手层
   - 现象：当前回合和委派链路已有事件，但缺少统一、可持久化、可恢复的任务状态对象，助手重启后难回答“做到哪一步、下一步是什么、哪里失败了”。
   - 复现步骤：发起任务后重启应用或中断回合，检查是否存在稳定的 `Queued/Running/Blocked/Done/Failed/Canceled` 状态文件与恢复入口。
   - 影响范围：任务承接、后台委派跟进、结果解释、后续 UI 面板建设
   - 验收标准：新增统一任务状态持久化；至少覆盖 `Queued/Running/Blocked/Done/Failed/Canceled` 六态；回合排队、启动、失败、取消、后台委派等待、后台任务完成可驱动状态迁移；具备最小回归测试
   - 状态：accepted
   - 结论：已落地最小后端闭环：新增 `TaskStateService`、`task_state.updated` 事件与 `TaskStateServiceTest`；当前完成后端状态机基础，状态存储后续已从 `task_state.json` 收口到 SQLite `app_state`，后续继续推进 UI 展示与更细粒度编排规则

---

*文档版本：v1.7*
*创建日期：2026-02-11*
*关联文档：docs/30_执行/30-当前执行主线.md、docs/00_规范/03-文档执行闭环规范.md*
